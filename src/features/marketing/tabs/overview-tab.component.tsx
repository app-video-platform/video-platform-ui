import React from 'react';

import { PageHeader } from '@shared/components';

const OverviewTab: React.FC = () => (
  <div>
    <PageHeader
      title="Overview"
      subTitle="Manage marketing for your products, see upcoming live sessions, and reply to customer reviews"
    />
  </div>
);

export default OverviewTab;
