import React from 'react';

import { PageHeader } from '@components';

const SessionsTab: React.FC = () => (
  <div>
    <PageHeader
      title="Live Sessions"
      subTitle="Manage and schedule your next live sessions"
    />
  </div>
);

export default SessionsTab;
